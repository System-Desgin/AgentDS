import { notImplemented } from "./not-implemented";

/**
 * Fetch a system's token package and normalize it to the DESIGN.md schema
 * with provenance (Phase 1, F-6). Adapters: npm-tokens, repo-json, css-analysis.
 */
export function runExtract(_slug: string): void {
  notImplemented("extract");
}
