import { notImplemented } from "./not-implemented";

/**
 * Validate an entry: meta.yaml against the shared schema and DESIGN.md via
 * `npx @google/design.md lint`; persist the report; block on errors
 * (Phase 1, F-6/F-8).
 */
export function runValidate(_slug: string): void {
  notImplemented("validate");
}
