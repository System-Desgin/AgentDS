import { notImplemented } from "./not-implemented";

/**
 * Export tokens.json (DTCG) + tailwind.css via the official CLI, and build
 * bundle.zip including LICENSE-NOTICE.txt (Phase 1, F-6).
 */
export function runExport(_slug: string): void {
  notImplemented("export");
}
