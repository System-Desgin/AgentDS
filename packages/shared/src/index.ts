/**
 * @agentds/shared — the contract shared by web, api, and the pipeline.
 * zod schemas + inferred types + the purpose taxonomy. Never duplicate these
 * definitions elsewhere (CLAUDE.md); import from here.
 */
export * from "./taxonomy";
export * from "./schemas";
export * from "./types";
export * from "./content";
